{
  "name": "Node.js & pnpm Dev Container",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:24-trixie",
  
  "features": {
    "ghcr.io/devcontainers/features/node:2.0.0": {
      "version": "none",
      "pnpmVersion": "latest"
    }
  },

  // Injects the npm blocker into both bash and zsh when the container starts
  "postCreateCommand": "echo 'alias npm=\"echo \\\"❌ ERROR: Use pnpm instead of npm!\\\" && false\"' >> ~/.bashrc && echo 'alias npm=\"echo \\\"❌ ERROR: Use pnpm instead of npm!\\\" && false\"' >> ~/.zshrc",

  "customizations": {
    "vscode": {
      "extensions": [
        "linbudu.pnpm-vscode-helper"
      ]
    }
  },

  "remoteUser": "node"
}